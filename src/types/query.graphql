type Query {
  products: [Product]
  product(id: ID!): Product
  pcmProducts(token: String): [PCMProduct]
  pcmProduct(id: ID!, token: String): PCMProduct
  brands: [Brand]
  brand(id: ID!): Brand
  collections: [Collection]
  collection(id: ID!): Collection
  categories: [Category]
  category(id: ID!): Category
  cart(id: ID!): Cart
  customer(id: ID!, token: String!): Customer
  customerAddresses(customer: String!): [CustomerAddress]
  customerAddress(customer: String!, address: String!): CustomerAddress
}
